Get a number from user and check if the number equals 50. If yes print "sucess" else print "failure"

#include<stdio.h>
int main()
{
    int x;
    printf("enter a number:");
    scanf("%d",&x);
    if(x==50)
    {
        printf("success");
    }
    else
    {
        printf("failure");
    }
}

Results:
enter a number:45
failure

enter a number:50
success

