Get a number, Check if number less than or equal to 50. if yes print "success" else "failure".

#include<stdio.h>
int main()
{
    int x;
    printf("enter a number:");
    scanf("%d",&x);
    if(x<=50)
    {
        printf("success");
    }
    else
    {
        printf("failure");
    }
}

Result:

enter a number:45
success

enter a number:80
failure
