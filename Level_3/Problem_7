Get a  2-digit number and check if the digit 0 and digit 1 are identical. If yes print "sucess" else "failure".

#include<stdio.h>
int main()
{
    int x;
    printf("enter a number:");
    scanf("%d",&x);
    if((x%10)==(x/10))
    {
        printf("success");
    }
    else
    {
        printf("failure");
    }
}

Result:

enter a number:50
failure

enter a number:55
success

