Get a number and check if the number is not equal to 50. If yes print "sucess" else "failure".

#include<stdio.h>
int main()
{
    int x;
    printf("enter a number:");
    scanf("%d",&x);
    if(x!=50)
    {
        printf("success");
    }
    else
    {
        printf("failure");
    }
}

Result:

enter a number:50
failure

enter a number:45
success
