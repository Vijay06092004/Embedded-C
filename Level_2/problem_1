Get a number from the user and check if the number equals to 50 if yes the print 1, otherwise print 0

Example: 
        input : 50    output : 1
        input : 56789 output : 0

code : 

#include<stdio.h>
int main() { 
    int num,result;
    printf("enter a number : ");
    scanf("%d",&num);
    result = (num==50);
    printf("the result is %d", result);
    return 0;
}

result : 
        input : 50 output : 1
        input : 56789 output  : 0
