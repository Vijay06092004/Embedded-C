Question : Get a two digit number from the user and print the ones digit 

Example : 
input : 45   output : 5
input : 56   output : 6

code : 
#include <stdio.h>

int main() {
    int number, ones_digit;
    printf("Enter a two-digit number: ");
    scanf("%d", &number);
    ones_digit = number % 10;
    printf("The ones digit is: %d\n", ones_digit);
    return 0;
}

output :    
Enter a two-digit number:45     The ones digit is: 5    
Enter a two-digit number:56     The ones digit is: 6
